const signup = () => {
  // take user info from form
  // validation
  // db connect
  // already exits check user
  //  insert in db
  console.log("signup");
};

signup();

// api haru node ma banaune

const login = () => {
  // take user info from form
  // validation
  // db connect
  // already exits check user
  //  insert in db
  console.log("signup");
};

login();

module.export = {
  login: login,
  signup,
};
